// add styles
// 主函数
// three.js
//TODO
/**
 *  如果是 添加库  需要挂载到window  下面
 */
import * as THREE from 'three'
window["THREE"] = THREE
import * as TWEEN from '@tweenjs/tween.js'
window["TWEEN"] = TWEEN
/*****start load js ****/







/*****load js end   ****/

export class Main{constructor(){}public run(){App.run()}}new Main().run();window["Main"] = Main
// requestAnimationFrame(Animate.run)
// function animate(): void {requestAnimationFrame(animate);render()}
// function render(): void {App.renderer.render(App.scene, App.camera)}
// animate()






